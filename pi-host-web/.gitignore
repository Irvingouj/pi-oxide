# Raw WASM bindings — regenerated by the release workflow (cargo + wasm-bindgen)
# from source. Never commit; the published npm tarball carries them.
/pi_host_web.js
/pi_host_web.d.ts
/pi_host_web_bg.wasm
/pi_host_web_bg.wasm.d.ts

# vite build output
/dist/
