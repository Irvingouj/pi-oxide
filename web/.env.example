# Copy this to .env.development and fill in your credentials.
# These are only used by `npm run dev` (Vite dev server).
# They are NOT included in production builds.

VITE_API_KEY=
VITE_BASE_URL=https://api.fireworks.ai/inference
VITE_MODEL=accounts/fireworks/routers/kimi-k2p6-turbo
